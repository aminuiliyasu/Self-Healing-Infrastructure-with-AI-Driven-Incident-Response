apiVersion: v1
kind: Service
metadata:
  name: self-healing-app
  labels:
    app: self-healing-app
spec:
  type: NodePort
  selector:
    app: self-healing-app
  ports:
    - name: http
      port: 5000
      targetPort: 5000
      nodePort: 30050 # overlays override this per environment
